# Say It, Write It

**Say It, Write It** (aka Siwi) is an extremely simple Closed Captioning overlay meant to be used with streaming software like OBS and Streamlabs.

## Building the project

```
uv run pyinstaller main.spec
```